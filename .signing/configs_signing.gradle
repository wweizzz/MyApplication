project.apply plugin: 'kotlin-android'

android {
    signingConfigs {
        debug {
            storeFile file("../.signing/app.jks")
            storePassword "123456"
            keyAlias "app"
            keyPassword "123456"
        }
        release {
            storeFile file("../.signing/app.jks")
            storePassword "123456"
            keyAlias "app"
            keyPassword "123456"
        }
    }
}